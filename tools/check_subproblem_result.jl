
# placeholder. Apparently the AI can't retrieve this value on its own
# Might be updated as soon as proper parameters for failure (e.g. running out of time) are introduced
function subproblem_succeeded(status, has_vals::Bool)
    return has_vals
end